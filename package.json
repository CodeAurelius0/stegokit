{
  "name": "stegokit",
  "version": "1.0.0",
  "description": "Production-ready Steganography Toolkit — hide images & text in carrier images",
  "private": true,
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  },
  "scripts": {
    "start": "npm --prefix backend start",
    "build": "npm --prefix frontend run build",
    "backend": "npm --prefix backend start",
    "backend:dev": "npm --prefix backend run dev",
    "frontend": "npm --prefix frontend run dev",
    "test": "npm --prefix backend test",
    "install:all": "npm --prefix backend install && npm --prefix cli install && npm --prefix frontend install",
    "postinstall": "npm --prefix backend install && npm --prefix frontend install"
  }
}
